import React from 'react'
import { View, Text } from 'react-native'

export default function Sair() {

    return (
        <View style={{minHeight: 500, alignItems: "center", justifyContent: "center"}}>
            <Text style={{fontSize: 16, fontWeight: "bold"}}>Saiu da sessão !</Text>
        </View>
    )
}